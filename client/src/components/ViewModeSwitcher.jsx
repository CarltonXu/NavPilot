import React from 'react';
import Icon from './Icon.jsx';
import { useI18n } from '../i18n/LocaleContext.jsx';
export default function ViewModeSwitcher({ value, onChange }) { const { t } = useI18n(); return <div className="view-switch" aria-label={t('view.label')}><button className={value === 'card' ? 'active' : ''} aria-pressed={value === 'card'} onClick={() => onChange('card')}><Icon name="grid" size={16} />{t('view.card')}</button><button className={value === 'compact' ? 'active' : ''} aria-pressed={value === 'compact'} onClick={() => onChange('compact')}><Icon name="list" size={16} />{t('view.compact')}</button><button className={value === 'board' ? 'active' : ''} aria-pressed={value === 'board'} onClick={() => onChange('board')}><Icon name="board" size={16} />{t('view.board')}</button></div>; }
