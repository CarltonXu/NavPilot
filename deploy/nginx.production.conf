worker_processes auto;
events { worker_connections 1024; }
http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  sendfile on;
  tcp_nopush on;
  keepalive_timeout 65;
  gzip on;
  gzip_comp_level 5;
  gzip_min_length 1024;
  gzip_vary on;
  gzip_types text/plain text/css application/javascript application/json application/xml image/svg+xml;
  proxy_buffering on;
  proxy_buffers 16 16k;
  proxy_buffer_size 16k;
  map $http_x_forwarded_proto $navpilot_forwarded_proto {
    default $http_x_forwarded_proto;
    "" $scheme;
  }
  upstream navpilot_api { server app:8787; keepalive 32; }
  server {
    # Preserve the existing Nginx Proxy Manager upstream: navpilot:8787.
    listen 8787;
    server_name _;
    root /usr/share/nginx/html;
    location /assets/ {
      try_files $uri =404;
      add_header Cache-Control "public, max-age=31536000, immutable" always;
    }
    location /uploads/ {
      proxy_pass http://navpilot_api;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $navpilot_forwarded_proto;
      proxy_buffering on;
      add_header Cache-Control "public, max-age=86400, stale-while-revalidate=604800" always;
    }
    location /api/ai/ {
      proxy_pass http://navpilot_api;
      proxy_http_version 1.1;
      proxy_set_header Connection "";
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $navpilot_forwarded_proto;
      proxy_buffering off;
      proxy_cache off;
      gzip off;
      add_header X-Accel-Buffering no;
    }
    location /api/ {
      proxy_pass http://navpilot_api;
      proxy_http_version 1.1;
      proxy_set_header Connection "";
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $navpilot_forwarded_proto;
      proxy_buffering on;
    }
    location / {
      proxy_pass http://navpilot_api;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $navpilot_forwarded_proto;
      proxy_buffering on;
    }
  }
}
