const express=require('express');const db=require('../db');const{requireUser,requirePasswordChanged}=require('../middleware/auth');const{auditWith}=require('../services/eventService');const{createTransferService}=require('../services/resourceTransferService');
const router=express.Router(),transfer=createTransferService(db);router.use(requireUser,requirePasswordChanged);function send(res,error){return res.status(error.status||400).json({code:error.code||'TRANSFER_FAILED',error:error.message});}
function currentRealm(req){const scope=req.body.scope==='public'?'public':'personal';if(scope==='public'&&req.auth.user.role!=='admin')throw Object.assign(new Error('只有管理员可以操作公共空间'),{code:'FORBIDDEN',status:403});return{scope,ownerId:scope==='personal'?req.auth.user.id:null};}
router.post('/export',(req,res)=>{try{const current=currentRealm(req),payload=transfer.selectionSnapshot(current,req.body.selection||{all:true});auditWith(db,req,'resources.exported',{targetType:`${current.scope}_space`,metadata:{scope:current.scope,itemCount:payload.items.length,categoryCount:payload.categories.length}});res.json(payload);}catch(error){send(res,error);}});
router.post('/preview',(req,res)=>{try{res.json(transfer.previewImport(currentRealm(req),req.body.payload,req.body.format||'auto'));}catch(error){send(res,error);}});
router.post('/import',(req,res)=>{try{const current=currentRealm(req),result=db.transaction(()=>{const value=transfer.importNormalized(current,req.body.payload,{selectedKeys:req.body.selectedKeys,targetCategoryId:req.body.targetCategoryId??null,preserveStructure:req.body.preserveStructure!==false,returnIds:true});const{importedIds,...summary}=value;auditWith(db,req,'resources.imported',{targetType:`${current.scope}_space`,metadata:{scope:current.scope,...summary}});return value;})();res.json(result);}catch(error){send(res,error);}});
module.exports=router;
